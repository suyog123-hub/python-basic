# use lambda function if we want tio make the code in the one line 
# use the lambda fun is we want to pass the function as an argument 


#lambda function 
# SYNTAX:
#function = lambda  , variable : to do task(block of code )


'''double = lambda x: x*2
cube = lambda x: x*x*x
avg = lambda x,y : (x+y)/2
a=(avg(4,2))
a=int(a)
print(a)'''

