#tuple ---> tuple is the collection of data
# it is in order
# it is immutable value cannot be changed in the tuple 
# it is created using the small bracket 
#it accept the multiple dublication of the data 

# tuple only have two metohd that is 
   # count method  is used to count the dublication of the value in the tuple 
   # index method is used to find the index of the valeu in the tuple 


tuple_data = (1,2,3,4,5)
print(tuple_data.index(1))
print(tuple_data.count(2))
